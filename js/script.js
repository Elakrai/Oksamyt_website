$(document).ready(function(){
	var owl=$('#slide_carousel');
	owl.owlCarousel({
		loop:true,
		margin: 0,
		autoplay: true,
		items: 1,
		autoplayTimeout:5000,
		autoplaySpeed: 1000
	});
});
